# Cloud Build configuration for webhook listener service
# This file defines the CI/CD pipeline for automated deployment to Cloud Run

steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build', 
      '-t', 'gcr.io/$PROJECT_ID/vocoeng-webhook-listener:$COMMIT_SHA',
      '-t', 'gcr.io/$PROJECT_ID/vocoeng-webhook-listener:latest',
      '.'
    ]
    dir: 'vocoeng-webhook-listener'

  # Push the image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/vocoeng-webhook-listener:$COMMIT_SHA']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/vocoeng-webhook-listener:latest']

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args: [
      'run', 'deploy', 'vocoeng-webhook-listener',
      '--image', 'gcr.io/$PROJECT_ID/vocoeng-webhook-listener:$COMMIT_SHA',
      '--region', 'us-central1',
      '--platform', 'managed',
      '--allow-unauthenticated',
      '--port', '8080',
      '--memory', '512Mi',
      '--cpu', '1',
      '--min-instances', '0',
      '--max-instances', '10',
      '--concurrency', '80',
      '--timeout', '300s',
      '--set-env-vars', 'PROJECT_ID=$PROJECT_ID,PUBSUB_TOPIC=vocoeng-messages',
      '--service-account', 'vocoeng-webhook-listener@$PROJECT_ID.iam.gserviceaccount.com'
    ]

# Store images in Container Registry
images:
  - 'gcr.io/$PROJECT_ID/vocoeng-webhook-listener:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/vocoeng-webhook-listener:latest'

# Build options for better performance
options:
  machineType: 'E2_HIGHCPU_8'
  diskSizeGb: 100
  logging: CLOUD_LOGGING_ONLY
